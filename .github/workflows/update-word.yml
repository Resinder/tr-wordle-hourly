name: Update Hourly Word

on:
  schedule:
    - cron: '0 * * * *' # Her saat başı
  workflow_dispatch: # Manuel tetikleme

permissions:
  contents: write

jobs:
  update-word:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Select New Word
        run: |
          node << 'EOF'
          const fs = require('fs');
          const https = require('https');

          const GIST_URL = "https://gist.githubusercontent.com/Resinder/b2897fd639006e34a1bf54252d730f7b/raw/b29034e404094142bfeb896e7e8e5aa50b6db46f/tdk-5-harfli-kelimeler.json";
          const FILE_NAME = 'gizli-kelime.json';

          https.get(GIST_URL, (res) => {
            let data = '';
            res.on('data', (chunk) => data += chunk);
            res.on('end', () => {
              try {
                const kelimeler = JSON.parse(data);
                
                let eskiKelime = "";
                if (fs.existsSync(FILE_NAME)) {
                  try {
                    const currentFile = JSON.parse(fs.readFileSync(FILE_NAME, 'utf-8'));
                    eskiKelime = currentFile.kelime;
                  } catch (e) {
                    console.log("Mevcut dosya okunamadı, yeni oluşturuluyor.");
                  }
                }

                let yeniKelime;
                do {
                  const index = Math.floor(Math.random() * kelimeler.length);
                  // Düzeltme: .toLocaleUpperCase('tr-TR') kullanılmalı
                  yeniKelime = kelimeler[index].toLocaleUpperCase('tr-TR');
                } while (yeniKelime === eskiKelime && kelimeler.length > 1);

                const output = {
                  kelime: yeniKelime,
                  saat: new Date().toISOString(),
                  timestamp: Date.now()
                };

                fs.writeFileSync(FILE_NAME, JSON.stringify(output, null, 2));
                console.log(`✅ Eski: ${eskiKelime} -> Yeni: ${yeniKelime}`);
              } catch (e) {
                console.error("Hata:", e.message);
                process.exit(1);
              }
            });
          }).on('error', (err) => {
            console.error("İndirme Hatası:", err.message);
            process.exit(1);
          });
          EOF

      - name: Commit and push changes
        run: |
          git config --local user.name "github-actions[bot]"
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          
          # Değişiklik kontrolü ve push
          git add gizli-kelime.json
          if git diff --cached --quiet; then
            echo "Değişiklik yok, commit yapılmıyor."
          else
            git commit -m "🔄 Kelime Güncellendi: $(date -u +'%Y-%m-%d %H:%M UTC')"
            git pull --rebase
            git push
          fi
